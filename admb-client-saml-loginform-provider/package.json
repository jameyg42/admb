{
  "name": "@admb/client-saml-loginform-provider",
  "version": "4.0.0",
  "description": "@admin/client LoginProvider for SAML providers that authenticate via Login forms",
  "author": "Jamey Graham <jameyg228@gmail.com>",
  "license": "MIT",
  "main": "out/index.js",
  "types": "out/index.d.ts",
  "files": [
    "/out"
  ],
  "scripts": {
    "clean": "rimraf ./out",
    "prebuild": "npm run clean",
    "build": "tsc",
    "build:incremental": "tsc --incremental",
    "watch": "tsc --watch",
    "pretest": "npm run build",
    "test": "jest",
    "prepublish": "npm run build"
  },
  "dependencies": {
    "@admb/client": "*",
    "http-cookie-agent": "^5.0.2",
    "node-html-parser": "^6.1.1",
    "tough-cookie": "^4.0.0"
  },
  "devDependencies": {
    "@types/tough-cookie": "^4.0.2",
    "jest": "27.5.1",
    "rimraf": "^5.0.0",
    "typescript": "~4.8.2"
  }
}
