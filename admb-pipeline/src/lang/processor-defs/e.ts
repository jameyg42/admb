import { CommandDescription } from "./api";

export const e:CommandDescription = {
    name: 'e',
    documentation: 'Raises each value in the series to `e` (the base for natural logarithms).',
    arguments: []
};
