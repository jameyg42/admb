import { CommandDescription } from "./api";

export const integral:CommandDescription = {
    name: 'integral',
    documentation: 'Continually sums each subsequent data point.  Inverse of `derivative`.',
    arguments: []
};
