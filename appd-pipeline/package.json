{
  "name": "@metlife/appd-pipeline",
  "version": "2.0.0",
  "description": "AppD metrics pipeline (grammar and runtime)",
  "private": true,
  "author": "Jamey Graham <jgraham@metlife.com>",
  "license": "UNLICENSED",
  "main": "out/index.js",
  "scripts": {
    "clean": "rimraf ./out",
    "prebuild": "npm run clean",
    "build": "mkdir out && lezer-generator src/pipeline.grammar --cjs --names -o out/_parser && tsc",
    "watch": "tsc --watch",
    "pretest": "npm run build",
    "test": "jest",
    "prepublish": "npm run build"
  },
  "dependencies": {
    "@lezer/lr": "^0.15.0"
  },
  "devDependencies": {
    "jest": "^27.4.7",
    "rimraf": "^3.0.2",
    "typescript": "^4.3.5",
    "@lezer/generator": "^0.15.0"
  }
}
