import { CommandDescription } from "./api";

export const percentOf:CommandDescription = {
    name: 'percentOf',
    documentation: 'Takes the percentage of each subsequent series relative to the first series in each group.  See `reduce` for more information.',
    arguments: []
};
