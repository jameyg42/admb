import { CommandDescription } from "./api";

export const derivative:CommandDescription = {
    name: 'derivative',
    documentation: 'Calculates the difference between each subsequent data point.   Inverse of `integral`.',
    arguments: []
};
