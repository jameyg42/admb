import { CommandDescription } from "./api";

export const def:CommandDescription = {
    name: 'def',
    documentation: 'defines variables that can be subsequently used in the pipeline',
    arguments: []
};
