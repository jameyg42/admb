import { CommandDescription } from "./api";

export const abs:CommandDescription = {
    name: 'abs',
    documentation: 'Sets the absolute value for each value in the series. Useful when combined with `derivative` to show the change regardless of change direction.',
    arguments: []
}
