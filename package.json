{
  "name": "@metlife/appdynamics-js",
  "private": true,
  "license": "UNLICENSED",
  "author": "Jamey Graham <jgraham@metlife.com>",
  "repository": {
    "type": "git",
    "url": "https://devtools.metlife.com/bitbucket/scm/~jgraham/appd-js.git"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "build:incremental": "npm run build:incremental --workspaces --if-present",
    "package": "npm run build && npm run package:only",
    "package:only": "npm run package --workspaces --if-present",
    "package:incremental": "npm run build:incremental && npm run package:only",
    "publish": "npm publish --workspace appd-libutils --workspace appd-libstats --workspace appd-libmetrics --workspace appd-client --workspace appd-services --workspace appd-pipeline",
    "debug:server": "nodemon admb-server/src/index.ts",
    "debug:ui": "npm run watch --workspace admb-ui",
    "debug": "npm-run-all -p -r debug:server debug:ui"
  },
  "workspaces": [
    "appd-libutils",
    "appd-libstats",
    "appd-client",
    "appd-libmetrics",
    "appd-services",
    "appd-pipeline",
    "admb-lang",
    "admb-ui",
    "admb-server",
    "admb-lang-tester"
  ],
  "devDependencies": {
    "nodemon": "^3.0.1",
    "npm-run-all": "^4.1.5",
    "ts-node": "^10.8.0"
  }
}
