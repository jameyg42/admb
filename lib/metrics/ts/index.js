module.exports = {
    reduce:  require('./reduce'),
    extract: require('./extract').extract,
    asPercent: require('./asPercent').asPercent,
    apply: require('./apply'),
    subseries: require('./subseries')
}
